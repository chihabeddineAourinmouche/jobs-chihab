		<script type="text/javascript">

			var search_text_zone = document.getElementById('search_text_zone');
			
			function search_it(){
				var search_text_zone_value = search_text_zone.value;
				console.log(search_text_zone_value);
				window.location.replace('/' + search_text_zone_value);
			}

			search_text_zone.addEventListener('keydown', (event) => {
				if (event.key === "Enter") {
				    event.preventDefault();
				    var search_text_zone_value = search_text_zone.value;
					window.location.replace('/' + search_text_zone_value);
				}
			});

			function show_all(){
				window.location.replace('/');
			}
			
			function search_it_arg(tag){
				console.log(tag);
				window.location.replace('/' + tag);
			}
		</script>
		<script src="/bower_components/jquery/dist/jquery.js"></script>
		<script src="/bower_components/bootstrap/dist/js/bootstrap.js"></script>
	</body>
</html>